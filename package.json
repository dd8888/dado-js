{
  "name": "dado",
  "version": "1.0.0",
  "description": "Prueba dado",
  "main": "index.js",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url /dado-js/ && gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist build .cache gh-pages-clean"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
